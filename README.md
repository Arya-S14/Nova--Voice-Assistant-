# Nova--Voice-Assistant-
NOVA · AI Voice Assistant that actually does things. Speak a command → local LLM reasons → real action executes. Built with Groq + LLaMA3 + Web Speech API. No cloud dependency. No fluff.
